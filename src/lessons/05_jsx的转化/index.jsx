// import React from 'react'

// jsx的本质：jsx最终会被转换成react.createelement的函数调用
// createelement（）用来创建一个element对象，且createelement需要三个参数
// type:对应的标签 ，config:标签的对应属性，children：标签里面的存储信息
// 1462-1463源码解析
export default function Index() {
  return (
    <div>Index</div>
  )
}
